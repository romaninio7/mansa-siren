import * as React from 'react';
import { storiesOf } from '@storybook/react';
import UserProfile from 'components/UserProfile';

storiesOf('UserProfile', module).add('Component', () => <UserProfile />);
