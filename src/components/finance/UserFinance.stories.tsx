import * as React from 'react';
import { storiesOf } from '@storybook/react';
import UserFinance from 'components/finance/UserFinance';

storiesOf('UserFinance', module).add('Component', () => <UserFinance />);
