import * as React from 'react';
import { storiesOf } from '@storybook/react';
import UserBusiness from 'components/business/UserBusiness';

storiesOf('UserProfile/UserBusiness', module).add('Component', () => (
  <UserBusiness />
));
